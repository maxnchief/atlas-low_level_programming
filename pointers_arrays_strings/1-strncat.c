#include <stdio.h>
#include "MAIN_H"
/**
 * _strncat - concanates the strings
 *
 * @i - parameter
 *
 * @j - parameter
 *
 * Return: 0
 */

char *_strncat(char *dest, char *src, int n)

{
	int i, j = strlen(s);

	for (i = 0; s1[i] != '\0'; i++)
	{
		s[i + j] = s1[i];
	}

	s[i + j] = '\0';
}
