#include "main.h"
#include <stdio.h>
/**
* _abs - function to find the absolute value of a number
* @a: Function parameter
* Return: a or -a
*/

int _abs(int a)
{
	if (a < 0);
	else if (a > 0)
	{
		return (a);
	}
	return (-a);
}
